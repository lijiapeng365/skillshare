# 应用服务 WEB 访问端口
server.port=8080

# 数据库配置
spring.datasource.url=jdbc:mysql://123.249.124.74:3306/skillshare?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.cloudladdertech.skillshare.entity
mybatis.configuration.map-underscore-to-camel-case=true

# JWT配置
jwt.secret=skillshare_secret_key
jwt.expiration=3600

# 日志配置
logging.level.com.cloudladdertech.skillshare.mapper=debug

# Swagger配置
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

